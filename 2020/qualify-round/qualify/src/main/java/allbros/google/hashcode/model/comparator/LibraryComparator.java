package allbros.google.hashcode.model.comparator;

import allbros.google.hashcode.model.Library;

public class LibraryComparator implements Comparable<Library> {
    @Override
    public int compareTo(Library library) {
        return 0;
    }
}
