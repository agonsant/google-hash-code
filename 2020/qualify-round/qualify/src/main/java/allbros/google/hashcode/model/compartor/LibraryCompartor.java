package allbros.google.hashcode.model.compartor;

import allbros.google.hashcode.model.Library;

public class LibraryCompartor implements Comparable<Library> {
    @Override
    public int compareTo(Library library) {
        return 0;
    }
}
